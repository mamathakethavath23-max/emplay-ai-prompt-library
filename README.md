# AI Prompt Library (Submission Ready)

Full-stack assignment project using Angular + Django + PostgreSQL + Redis + Docker Compose.

## Run
```bash
docker compose up --build
```

Frontend: http://localhost:4200  
Backend: http://localhost:8000

## Features
- List prompts
- Add prompt
- View prompt detail
- Redis view counter
- Dockerized services
